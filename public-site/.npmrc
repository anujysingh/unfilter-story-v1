# react-day-picker@8 declares a peer dep of react <=18, but the project runs
# react 19 (works fine at runtime). Allow the install to proceed instead of
# failing on the peer-dep mismatch. Required for Vercel's `npm install` to pass.
legacy-peer-deps=true
